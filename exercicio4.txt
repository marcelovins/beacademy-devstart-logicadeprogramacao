# Exercício 04 - Python

"""class Conta:


    def __init__(self, saldo):
        self.__saldo = saldo

    def extrato(self):
        return f'Seu saldo é de {self.__saldo}'

    def depositar(self, valor):
        self.__saldo += valor

    def sacar(self, valor):
        self.__saldo -= valor


saldo_inicial = int(input(f'digite seu saldo inicial:'))
deposito = int(input(f'digite o valor que deseja depositar:'))

conta1 = Conta(saldo_inicial)
print(conta1.depositar(deposito))
print(conta1.extrato())"""